# shared-os-app-mac-ios

**Persona-side** Matraix Playground environment stub for os-app tasks on macOS / iOS via
**use.computer** (remote desktop / simulator). No local Docker desktop image.

Does not host a product SUT — the app under test lives on the remote sandbox.
