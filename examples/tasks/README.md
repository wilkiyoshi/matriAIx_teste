# Example Tasks

These tasks are generic Matraix Playground examples, not PersonaBench benchmark tasks.

Included:

- `hello-world/`: a no-API-key smoke task that asks an oracle agent to create
  `hello.txt`.
