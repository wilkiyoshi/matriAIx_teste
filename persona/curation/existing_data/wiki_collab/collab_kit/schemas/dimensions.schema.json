{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Persona dimension spec",
  "description": "dimensions.json — the closed-vocabulary spec the owner ships with the tasks. Each result field_id must be one of these ids, and each non-null value must be one of that id's allowed values.",
  "type": "array",
  "items": {
    "type": "object",
    "required": ["id", "values"],
    "properties": {
      "id": { "type": "string" },
      "label": { "type": "string" },
      "description": { "type": "string" },
      "category": { "type": "string", "description": "Used for batching/grouping; optional." },
      "values": {
        "type": "array",
        "items": { "type": "string" },
        "description": "The allowed values for this dimension. A result value must be one of these (verbatim) or null."
      }
    }
  }
}
