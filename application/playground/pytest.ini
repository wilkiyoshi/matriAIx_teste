[pytest]
# Scope collection to this app's own test suites. Pinning testpaths keeps
# `pytest -q` focused on the harness code we own.
testpaths =
    backend/tests
norecursedirs = .venv .git frontend node_modules
